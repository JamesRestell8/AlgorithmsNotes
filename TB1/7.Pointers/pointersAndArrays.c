#include <stdio.h>
#include <stdlib.h>

// An array occupies contiguous bytes of memory.
// In C, the array name is equal to the pointer pointing to the first element in the array.

// when we pass an array to a function, we are actually passing a pointer to the first element of the array
// hence why arrays are passed by reference

int main()
{
    return 0;
}